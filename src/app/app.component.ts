import { Component } from '@angular/core';

// Componente root que se manda a plantilla principal HTML
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
})
export class AppComponent {
  title = 'gifs-App';
}
